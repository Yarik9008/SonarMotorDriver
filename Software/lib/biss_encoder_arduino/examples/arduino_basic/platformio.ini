; BiSS-C encoder example — Arduino (STM32duino)

[env:stm32f103c8]
platform  = ststm32
board     = genericSTM32F103C8
framework = arduino

lib_deps =
    file://../../

build_flags =
    -DSTM32F103xB
    -DPIO_FRAMEWORK_ARDUINO_ENABLE_CDC

monitor_speed = 115200
