"""Отладочный GUI для прошивки FW_SonarMotorDriver.

Пакет содержит:
- protocol   — единый источник правды по командам/телеметрии;
- transport  — абстракция канала (реальный COM или встроенный симулятор);
- simulator  — Python-модель прошивки для работы без железа;
- controller — связка транспорта и UI (валидация, таймауты, маршрутизация);
- widgets    — PPI-диаграмма, графики телеметрии и панели управления.
"""

__version__ = "1.0.0"
